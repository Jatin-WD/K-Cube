import KCubePage from '@/components/KCubePage';

export const dynamic = 'force-dynamic';
export const revalidate = 0;

const ActivitiesPage = () => {
  return <KCubePage pageKey="activities" />;
};

export default ActivitiesPage;
