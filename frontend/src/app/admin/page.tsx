import AdminCms from '@/components/AdminCms';

export const metadata = {
  title: 'Admin CMS | K-CUBE',
  description: 'K-CUBE admin CMS for managing content, users, points, events, lessons, rewards and Korea trip operations.',
};

const AdminPage = () => {
  return <AdminCms />;
};

export default AdminPage;
