cd /opt/K-Cube/K-Cube
pkill -f "node server.js"
git pull origin main
docker compose -f docker-compose.prod.yml up -d --build frontend backend nginx





verify:

docker compose -f docker-compose.prod.yml ps
curl -k https://k-cube.store+



OR

Use this one-liner on the server :

cd /opt/K-Cube/K-Cube && git pull origin main && docker compose -f docker-compose.prod.yml up -d --build frontend backend nginx && curl -k https://k-cube.store